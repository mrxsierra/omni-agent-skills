[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-validation-kit"
version = "0.0.1"
description = "Universal AI Agent Skill Registry & Security Harness"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    { name = "Sunil Sharma", email = "mrxsierra@users.noreply.github.com" }
]
keywords = [
    "ai-agent",
    "antigravity",
    "claude-code",
    "cursor",
    "agentic-skills",
    "security-shield",
    "mcp",
    "llms-txt"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries :: Python Modules"
]

[project.urls]
Homepage = "https://github.com/mrxsierra/agent-validation-kit"
Repository = "https://github.com/mrxsierra/agent-validation-kit.git"
Issues = "https://github.com/mrxsierra/agent-validation-kit/issues"

[project.scripts]
agent-validation = "scripts.build-registry:main"

[tool.hatch.build.targets.wheel]
packages = ["scripts"]
